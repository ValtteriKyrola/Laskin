import { theme } from './theme';

export const chartColors = theme.colors.chart;

export const chartTheme = {
  grid: { stroke: '#E9ECEF' },
  axis: { fill: '#6C757D' },
  tooltip: { bg: '#fff', border: '#DEE2E6', color: '#212529' },
};

export const chartThemeDark = {
  grid: { stroke: '#343A40' },
  axis: { fill: '#ADB5BD' },
  tooltip: { bg: '#212529', border: '#343A40', color: '#F8F9FA' },
};
